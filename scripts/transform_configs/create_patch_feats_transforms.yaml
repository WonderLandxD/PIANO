transforms:
  - name: Resize
    params:
      size: [224, 224]  # Resize image to 256x256
  - name: ToTensor     # Convert image to tensor
  - name: Normalize    # Normalize image
    params:
      mean: [0.485, 0.456, 0.406]  # ImageNet mean
      std: [0.229, 0.224, 0.225]   # ImageNet std 